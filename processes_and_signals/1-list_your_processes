#!/usr/bin/env bash
# This script displays all running processes for all users, including those without a TTY, in a user-oriented format, showing process hierarchy.

# Display the header
echo "USER       PID %CPU %MEM    VSZ   RSS TTY      STAT START   TIME COMMAND"

# Display all running processes in a user-oriented format with process hierarchy
ps -eo user,pid,pcpu,pmem,vsz,rss,tty,stat,start,time,command --forest | head -50

